# Memory Game
Progetto Java MVC con build Maven, Docker e GitHub Actions.

## Comandi
- Build & Test: `mvn clean verify`
- Run: `java -jar target/memory-game-1.0-SNAPSHOT.jar`
- Docker: `docker-compose up --build`